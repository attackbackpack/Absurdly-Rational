{
  "name": "absurdly-rational-site",
  "private": true,
  "version": "1.0.0",
  "description": "Content validation and image optimization for the Absurdly Rational static site.",
  "scripts": {
    "validate:content": "node scripts/validate-content.js",
    "optimize:images": "node scripts/optimize-images.js",
    "check": "npm run validate:content",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.9"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
